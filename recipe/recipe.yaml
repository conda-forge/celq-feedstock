context:
  version: "0.7.1"

package:
  name: celq
  version: ${{ version }}

source:
  url: https://github.com/IvanIsCoding/celq/archive/v${{ version }}.tar.gz
  sha256: 3df2699cb5aa7ad07ca94e1f83845aea96998e442dd764177062308fccdcbd13

build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
      - if: unix
        then:
          - cargo auditable install --locked --no-track --bins --root ${{ PREFIX }} --path .
        else:
          - cargo auditable install --locked --no-track --bins --root %LIBRARY_PREFIX% --path .
      - cargo-bundle-licenses --format yaml --output ./THIRDPARTY.yml

requirements:
  build:
    - ${{ compiler('rust') }}
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - cargo-bundle-licenses
    - cargo-auditable

tests:
  - script:
      - celq -n -b --void "1 > 0"

about:
  license: MIT
  license_file:
    - LICENSE-MIT
    - THIRDPARTY.yml
  summary: A CEL command-line query tool for JSON data
  homepage: https://github.com/IvanIsCoding/celq
  repository: https://github.com/IvanIsCoding/celq
  documentation: https://docs.rs/celq/latest/celq/

extra:
  recipe-maintainers:
    - IvanIsCoding
